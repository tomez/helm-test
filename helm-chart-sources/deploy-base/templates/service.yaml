apiVersion: v1
kind: Service
metadata:
  name: {{ include "deploy-base.fullname" . }}
  labels:
    {{- include "deploy-base.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deploy-base.selectorLabels" . | nindent 4 }}
